<%= simple_form_for [@parking, @rent] do |f| %>

    <%= f.input :price %>

    <%= f.input :beginning %>

    <%= f.input :termination %>

    <%= f.button :submit, :class => 'btn btn-primary' %>
<% end %>
