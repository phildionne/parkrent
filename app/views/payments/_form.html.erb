<%= simple_form_for [@order, @payment], url: order_payment_path do |f| %>

  <%= f.input :amount %>

  <%= f.button :submit, data: { disable_with: 'Please wait ...' }, class: 'btn btn-primary' %>
<% end %>
