<%= simple_form_for(@vehicle, defaults: { label: false, input_html: { :class => 'transparent'}}) do |f| %>

  <%= render 'fields', f: f %>

  <%= f.button :submit, data: { disable_with: 'Please wait ...' }, :class => 'btn btn-primary' %>
<% end %>
