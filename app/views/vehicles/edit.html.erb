<h1>Editing vehicle</h1>

<%= render 'form' %>

<% if @vehicle.deletable_by?(current_user) %>
  <%= link_to 'Destroy', vehicle_path(@vehicle), method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>

<%= link_to 'Back', root_path %>
